# 새 컴퓨터 이전 검증 보고서

검증일: 2026-06-21 (Asia/Seoul)

> 이 문서는 진행 중인 검증 기록이다. 실시간 손동작, 조건별 모델 평가, 통합·장치 테스트가 끝나지 않았으므로 전체 상태는 **자동 검증 완료 / 실시간 검증 대기**이다.

## 1. 실행 환경

상태: **완료**

- OS: Windows, PowerShell
- Git: 2.54.0
- Python: 3.10.11 (`venv` 생성 완료)
- Node.js: 24.17.0 LTS
- npm: 11.13.0
- 브랜치: `handoff/kiosk-integration-20260621`
- 커밋: `9b744cc3dff365f8c0e470710a28ff6b6c6f3651`
- `.env`: 존재하며 필수 키가 모두 존재하고 비어 있지 않음을 값 노출 없이 확인
- DB: SQLite `kiosk.db`; MySQL 미설치

## 2. 모델 무결성 및 정책

상태: **완료**

- `app/ai/models/gesture_model.pkl`: `75a8bf05864297676048f50963a0a5aaff036cabe4ab31c9cf56fe2a732b6067`
- `app/ai/models/label_encoder.pkl`: `9bf71b1c50c14fb0691b81e23408517a936d404b6fd2e8825764009ea5361ab5`
- 실제 모델 입력: 63차원
- 실제 라벨 수: 32개
- `none` 클래스: 포함
- 모델/인코더 클래스 수: 일치
- 코드 정책: 신뢰도 0.80, 한 손, 감지 0.7, 추적 0.5, 웹 입력 `x -> 1-x` 확인
- 모델 재학습·교체·임계값 변경: 수행하지 않음

## 3. DB 및 자동 테스트

상태: **완료**

- SQLite Alembic 마이그레이션: 성공 (`20260620_01`)
- lint: 성공(ESLint, Ruff)
- React 테스트: 7 suites, 25 tests 모두 성공
- Python 테스트: 8 tests 모두 성공
- 전체 자동 테스트: 33 tests 성공, 실패 0
- 프로덕션 빌드: 성공
- npm 설치 감사 경고: 총 66건(낮음 5, 보통 38, 높음 22, 심각 1). 잠금파일을 바꾸는 자동 수정은 수행하지 않음.

## 4. 백엔드 실행 확인

상태: **완료**

- `/`: 200
- `/info`: 200, `model_loaded=true`
- `/api/health`: 200, DB `connected`, AI `loaded`, `all_systems_ready=true`
- `/docs`: 200
- 모든 응답에서 요청 ID 헤더 확인
- 참고: Alembic 실행 뒤 애플리케이션 INFO 로그가 콘솔에 충분히 남지 않는 로깅 특이점이 있으나, DB 마이그레이션·직접 라벨 매핑 검증·상태 API로 준비 상태를 교차 확인했다.

## 5. 프론트 실행 확인

상태: **자동 검증 완료 / 실시간 검증 대기**

- React 개발 서버 컴파일: 성공
- `/`: Chrome 실제 렌더링 성공(키오스크 입력·편집·전송 UI 확인)
- `/admin`: Chrome 실제 렌더링 성공(역무원 로그인 UI 확인)
- `/hardware-check.html`: HTTP 200
- 헤드리스 렌더링 중 브라우저 오류 표식 및 백엔드 네트워크 오류: 0건
- 실제 카메라 권한 안내와 장치 동작: 사용자 조작 대기

## 6. Python/웹 인식 비교 결과

상태: **완료**

- MediaPipe 0.10.14 및 Hands API import 성공
- 사용자 수동 검증 결과 Python과 웹 인식이 정상 작동함
- 지정 자모, 좌우 손 사용, `none` 중립 자세와 손 없음 동작을 정성적으로 통과한 것으로 기록
- 자모별 신뢰도 원시 수치는 별도로 수집하지 않았으므로 정량 비교값은 없음

## 7. 조건별 모델 정확도

상태: **추후 진행**

| 조건 | 전체 정확도 | 자모별 최저 정확도 | none 정확도 | 판정 |
|---|---:|---:|---:|---|
| normal | 미측정 | 미측정 | 미측정 | 사용자 결정으로 생략 |
| dim | 미측정 | 미측정 | 미측정 | 사용자 결정으로 생략 |
| near | 미측정 | 미측정 | 미측정 | 사용자 결정으로 생략 |
| far | 미측정 | 미측정 | 미측정 | 사용자 결정으로 생략 |

- 혼동 자모: 조건별 라이브 평가를 실행하지 않아 측정값 없음
- 사용자는 학습 당시 좌표 기반 데이터 증강을 수행했으므로 이번 조건별 라이브 평가를 생략하기로 결정했다. 데이터 증강과 실제 촬영 조건 평가는 목적이 다르므로 정확도 합격 판정은 내리지 않고 추후 진행으로 유지한다.
- `tester01`은 학습 데이터 수집 참여자이므로 독립 평가자가 아니다. 다른 평가자가 없으면 독립 평가는 추후 작업으로 남긴다.

## 8. 키오스크·역무원 통합 결과

상태: **완료**

- 손동작 문장 작성과 지우기, 공백, 초기화 기능 정상
- 문장 전송 후 관리자 화면에 역명, 위치, 시간, 완성 문장 전체 표시 정상
- 관리자 로그인, 빈 답변 방지, 답변 전송 정상
- 답변이 해당 키오스크에 표시되고 음성 출력 및 완료 후 폴링 종료 정상
- 답변 대기 중에는 전송 버튼이 `답변 대기 중`으로 잠겨 두 번째 질문을 동시에 보낼 수 없다. 이는 중복·혼선 방지를 위한 정상 동작으로 판정한다.
- 자동 API 테스트에서 서로 다른 두 장치의 질문·답변 분리도 통과했다.
- 사용자 제공 화면에서 전송된 질문 1건, 관리자 처리, 답변 표시 후 대기 목록 0건 복귀를 확인했다.

## 9. 장치 및 오류 테스트 결과

상태: **완료**

- 카메라·마이크·스피커 및 권한 관련 시나리오는 사용자 수동 검증에서 정상으로 보고됨
- 백엔드 중지, 잘못된 로그인, 음성 미지원 및 요청 ID 표시도 사용자 수동 검증에서 정상으로 보고됨
- 세부 장치 로그나 화면별 정량 자료는 별도로 수집하지 않음

## 10. 배포 준비 상태

상태: **추후 진행**

- 실제 배포 및 자동 시작 등록은 수행하지 않았다.
- `.env.production`: 없음
- Caddy 실행 파일 및 서비스: 미설치
- SQLite worker 1개 제한: 설정 검증 코드와 배포 실행 코드에서 확인
- 모델 자동 배치: `scripts/deploy/place_models.py` 존재, SHA-256·63차원·32클래스 검증 포함
- SQLite 백업: `scripts/deploy/backup_sqlite.py` 존재, 온라인 백업·SHA-256·30일 보존 지원
- 전체 화면 실행: `scripts/deploy/start_kiosk.ps1` 존재
- 자동 시작 작업: `scripts/deploy/install_startup_tasks.ps1` 존재하나 관리자 권한 및 사용자 승인 전이므로 실행하지 않음
- 공인 도메인 또는 `kiosk.local` 내부 인증서 방식 선택 대기
- `.env`의 관리자 비밀번호 자리표시자는 배포 전 실제 비밀값으로 변경해야 하며 실제 값은 보고서에 기록하지 않는다.

## 남은 작업

1. 조건별 라이브 평가는 사용자 결정으로 생략했으며 추후 필요 시 수행
2. 배포 준비 점검과 배포 방식 사용자 선택
